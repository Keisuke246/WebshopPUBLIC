# Local server
server:
  # port is used by spring-boot-admin
  port: 8080
  contextPath: /webshop
spring:
  datasource:
    password: c97daw
    url: jdbc:mysql://localhost:3306/webshop?user=root
    
